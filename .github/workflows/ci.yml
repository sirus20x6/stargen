name: CI

on:
  push:
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        compiler:
          - { name: gcc, cc: gcc-14, cxx: g++-14 }
          - { name: clang, cc: clang-18, cxx: clang++-18 }

    name: build-and-test (${{ matrix.compiler.name }})

    env:
      CC: ${{ matrix.compiler.cc }}
      CXX: ${{ matrix.compiler.cxx }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build cmake ${{ matrix.compiler.cxx }}

      - name: Configure
        run: cmake -S . -B build -G Ninja

      # NOTE: build only the specific targets. The `viewer` target is currently
      # broken, so a bare full build would make CI red.
      - name: Build stargen
        run: cmake --build build --target stargen -j

      - name: Build stargen_tests
        run: cmake --build build --target stargen_tests -j

      - name: Test
        run: ctest --test-dir build --output-on-failure
